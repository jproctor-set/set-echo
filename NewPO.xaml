<Page x:Class="SETEcho.NewPO"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SETEcho"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NewPO">

    <Page.DataContext>
        <local:PurchaseOrderViewModel/>
    </Page.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="PO Number:" Width="100"/>
            <TextBox Width="150" Text="{Binding Order.PONumber}"/>

            <TextBlock Text="Vendor:" Margin="20 0 0 0"/>
            <TextBox Width="200" Text="{Binding Order.Vendor}"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Order.Items}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item"
                                    Binding="{Binding Description}"/>
                <DataGridTextColumn Header="Qty"
                                    Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="Unit Price"
                                    Binding="{Binding UnitPrice}"/>
                <DataGridTextColumn Header="Total"
                                    Binding="{Binding Total}"
                                    IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>


        <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Right">
            <TextBlock Text="{Binding Order.Subtotal, StringFormat=Subtotal: {0:C}}"/>
            <TextBlock Text="{Binding Order.Tax, StringFormat=Tax: {0:C}}"/>
            <TextBlock Text="{Binding Order.GrandTotal, StringFormat=Total: {0:C}}"
                       FontWeight="Bold"/>
            <Button Content="Add Item"
                    Width="100"
                    Margin="0 10 0 0"
                    Command="{Binding AddItemCommand}"/>
        </StackPanel>
    </Grid>
</Page>
