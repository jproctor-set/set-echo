<Page x:Class="SETEcho.PurchaseOrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SETEcho"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PurchaseOrderPage">

    <Grid Background="#FAFAFA">


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <Border Grid.Column="1"
                Width="750" MaxHeight="450"
                Padding="16" Margin="0, 24"
                Background="White"
                BorderBrush="#DDD" BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <DockPanel Grid.Row="0" LastChildFill="False" Margin="0, 0, 0, 8">
                    <TextBlock Text="Purchase Orders" FontSize="20" FontWeight="SemiBold"
                               DockPanel.Dock="Left" VerticalAlignment="Center"/>

                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" >
                        <TextBox Width="240" Margin="0, 0, 8, 0"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Search PO # or Supplier"/>
                        <Button Content="Search" Margin="0, 0, 8, 0" Command="{Binding SearchCommand}"/>
                        <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
                    </StackPanel>
                </DockPanel>


                <TextBlock Grid.Row="1" Foreground="#666" FontSize="12" Margin="2, 0, 0, 6">
                    <Run Text="{Binding CountLabel}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding BusyLabel}"/>
                </TextBlock>


                <DataGrid 
                          Name="DataGridPO"      
                          Grid.Row="2"
                          ItemsSource="{Binding Orders}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          SelectionMode="Single"
                          AlternationCount="2" RowBackground="White" AlternatingRowBackground="Beige">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="PO #" Binding="{Binding PoNumber}" Width="120"/>
                        <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="120"/>
                        <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat={}{0:d}}" Width="120"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="80"/>
                    </DataGrid.Columns>



                    <DataGrid.Style>
                        <Style TargetType="DataGrid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Orders.Count}" Value="0">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGrid">
                                                <Grid>
                                                    <ContentPresenter/>
                                                    <TextBlock Text="No purchase orders found."
                                                               Foreground="#777" HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Style>
                </DataGrid>
            </Grid>
        </Border>

    </Grid>
</Page>
